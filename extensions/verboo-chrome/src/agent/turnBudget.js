// DECISÃO DO DONO (2026-08-18): 200 → 500 — turnos longos de navegador
// (listas grandes, extrações) estouravam o limite antes de concluir.
export const MAX_AGENT_STEPS = 500
export const MAX_AGENT_TURN_MS = 60 * 60_000
