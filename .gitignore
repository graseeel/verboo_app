node_modules/
out/
release/
build/
supabase/.temp/
supabase/.branches/
docs/superpowers/
.DS_Store
npm-debug.log*
/index.js
.env
.env.local

# Migration planning docs (local only)
plans/

# Tauri renderer build output (regenerated by `vite build`)
dist-renderer/

# Tauri Rust build artifacts (regenerated by `cargo`/`tauri build`)
src-tauri/target/
src-tauri/gen/
src-tauri/*.rlib

# Generated Verboo in Chrome sidecar binaries (target-specific)
src-tauri/binaries/verboo-in-chrome-*
# Generated pinned media sidecars (target-specific)
src-tauri/binaries/verboo-ffmpeg-*
src-tauri/binaries/verboo-ffprobe-*
src-tauri/binaries/verboo-whisper-*
src-tauri/binaries/verboo-node-*

# Tauri bundled CLI resource (copied by copy-cli-resource.mjs from node_modules)
src-tauri/resources/

# Maestri orchestration directory (local agent roles/config — not part of the app)
.maestri/

# Tauri Cargo config (has hardcoded local target-dir — must not be committed)
src-tauri/.cargo/

# Local agent/tool state
.superpowers/
.verboo/
extensions/verboo-chrome/dist/
extensions/verboo-chrome/store-assets/*.zip

# Editor config pessoal (Obsidian) — nao pertence ao repo
.obsidian/
test-results/
docs/installed-app.png
docs/flow-02.png
docs/vision-01.png
docs/vision-02.png
.playwright-mcp/
